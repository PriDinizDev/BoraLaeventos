@using Bem_vindo.pt.Models     <!-- use o SEU namespace aqui -->
@model Guide

}

<h1>@ViewData["Title"]</h1>

@{
    ViewData["Title"] = "Detalhes do Evento";
}
<h1>@ViewData["Title"]</h1>

@if (Model == null)
{
    <p>Evento não encontrado.</p>
}
else
{
    <dl class="row">
        <dt class="col-sm-3">Título do Evento</dt>
        <dd class="col-sm-9">@Model.Titulo</dd>

        <dt class="col-sm-3">Artista</dt>
        <dd class="col-sm-9">@Model.Entidade</dd>

        <dt class="col-sm-3">Tipo de Evento</dt>
        <dd class="col-sm-9">@Model.Processo</dd>

        <dt class="col-sm-3">Informações Gerais</dt>
        <dd class="col-sm-9">@Model.LinkGovernoOficial</dd>

        <dt class="col-sm-3">Link Oficial</dt>
        <dd class="col-sm-9">
            @if (!string.IsNullOrWhiteSpace(Model.LinkGovernoOficial))
            {
                <a href="@Model.LinkGovernoOficial" target="_blank" rel="noopener">Abrir link de compra</a>
            }
            else
            {
                <span>—</span>
            }
        </dd>
    </dl>

    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Editar</a>
    <a asp-action="Index" class="btn btn-secondary">Voltar</a>
}


    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Editar</a>
    <a asp-action="Index" class="btn btn-secondary">Voltar</a>

</div>
<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning">Editar</a> |
    <a asp-action="Index" class="btn btn-secondary">Voltar à Lista</a>
</div>